# Matching Service

## Responsibilities
- Live Dutch auction pricing calculation and surplus item reallocation.
- AWS API Gateway WebSocket connections (`$connect`, `$disconnect`) and real-time event broadcasting to connected frontend clients.
- DynamoDB auction inventory management and atomic item claiming transactions.
